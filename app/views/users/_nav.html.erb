<div class="d-flex justify-content-between bg-white p-2 mb-2">
  <h4>Menu</h4>
  <nav>
    <%= link_to 'Edit Profile', edit_user_path, class: 'btn btn-warning' %>
    <% if !user.eql?(current_user) %>
      <%= link_to 'Disable User', destroy_user_path(user), method: :delete, class: 'btn btn-danger' %>
    <% end %>
  </nav>
</div>
